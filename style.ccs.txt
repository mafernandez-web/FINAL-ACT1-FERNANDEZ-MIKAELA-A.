/* General Styles */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Changed font family for a slightly modern look */
    margin: 0;
    padding: 0;
    background-color: #e9eef2; /* Lighter, cooler background color */
    color: #36454F; /* Darker charcoal grey for default text */
    line-height: 1.6;
}

.container {
    max-width: 900px; /* Slightly wider content area */
    margin: 25px auto; /* Increased margin for better spacing */
    padding: 25px;
    background: #ffffff; /* White background for main content boxes */
    border-radius: 10px; /* Slightly more rounded corners */
    box-shadow: 0 4px 10px rgba(0,0,0,0.1); /* Softer, slightly larger shadow */
}

/* Navigation */
nav {
    background: #2c3e50; /* Darker, modern navy for navigation bar */
    padding: 12px 0;
    text-align: center; /* Centered navigation links */
}

nav ul {
    list-style: none;
    padding: 0;
    margin: 0; /* Removed default margin */
}

nav ul li {
    display: inline;
    margin: 0 20px; /* Increased spacing between links */
}

nav ul li a {
    color: #ecf0f1; /* Light grey for navigation links */
    text-decoration: none;
    font-weight: bold;
    transition: color 0.3s ease; /* Smooth color transition on hover */
}

nav ul li a:hover {
    color: #3498db; /* Bright blue on hover for navigation links */
}

/* Profile Picture */
.profile-pic {
    text-align: center;
    margin: 30px 0; /* More spacing around the picture */
}

.profile-pic img {
    width: 160px; /* Slightly larger picture */
    height: 160px;
    border-radius: 50%; /* Remains circular */
    object-fit: cover;
    border: 5px solid #3498db; /* Bright blue border around the picture */
    box-shadow: 0 0 15px rgba(0,0,0,0.2); /* Added a subtle shadow to the picture */
}

/* Sections */
section {
    margin: 40px 0; /* Increased spacing between sections */
}

h2 {
    color: #3498db; /* Bright blue for section titles */
    border-bottom: 2px solid #aec6cf; /* Lighter grey-blue line under titles */
    padding-bottom: 10px;
    margin-bottom: 20px; /* Increased space below titles */
    text-align: center; /* Centered section titles */
}

p {
    font-size: 1.05em; /* Slightly larger paragraph text */
    text-align: justify; /* Justified text for a cleaner look */
    padding: 0 15px; /* Added slight padding for text */
}

/* Skills Table */
table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px; /* Increased margin above table */
}

table, th, td {
    border: 1px solid #c0d9e9; /* Lighter, softer border for table cells */
}

th, td {
    padding: 15px; /* Increased padding in table cells */
    text-align: left;
}

th {
    background-color: #3498db; /* Bright blue background for table header */
    color: white;
    font-weight: bold;
}

tr:nth-child(even) {
    background-color: #f2f7fb; /* Light stripe for even rows in the table */
}

/* Contact Form */
form {
    display: flex;
    flex-direction: column;
    gap: 15px; /* Increased space between form elements */
    margin-top: 20px;
    padding: 0 15px; /* Added padding to align form with justified text */
}

label {
    font-weight: bold;
    margin-bottom: 5px;
    color: #36454F; /* Dark text for labels */
}

input, textarea {
    padding: 12px; /* Increased padding inside input fields */
    border: 1px solid #aebac9; /* Softer border for input fields */
    border-radius: 5px; /* Slightly more rounded input fields */
    font-size: 1em;
    width: calc(100% - 24px); /* Account for padding in width */
    box-sizing: border-box; /* Ensures padding is included in the element's total width and height */
}

textarea {
    resize: vertical; /* Allows users to resize the textarea vertically */
}

input::placeholder, textarea::placeholder {
    color: #999; /* Style for placeholder text */
}

button {
    padding: 15px 25px; /* Increased padding for a more substantial button */
    background: #3498db; /* Bright blue background for the button */
    color: white;
    border: none;
    border-radius: 5px; /* Slightly more rounded button corners */
    font-size: 1.1em; /* Larger font size for button text */
    font-weight: bold;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.2s ease; /* Smooth transition for hover effects */
    align-self: center; /* Center the button */
    max-width: 250px; /* Limit button width */
}

button:hover {
    background: #2980b9; /* Slightly darker blue on button hover */
    transform: translateY(-2px); /* Slight lift effect on hover */
    box-shadow: 0 4px 8px rgba(0,0,0,0.2);
}